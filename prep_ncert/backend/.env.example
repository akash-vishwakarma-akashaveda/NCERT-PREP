DATABASE_URL=postgres://user:password@localhost:5432/prep_ncert
PORT=4000
FRONTEND_ORIGIN=http://localhost:3000
GOOGLE_CLIENT_ID=
SESSION_SECRET=
SES_FROM_EMAIL=
AWS_REGION=ap-south-1
# Shared secret the Google Apps Script sends as X-Sync-Secret to POST /api/admin/sync-sheet.
# Leave blank to require an admin session instead (fine for manual syncs from the admin panel).
SHEET_SYNC_SECRET=
