# Firebase Web App Configuration
# Replace these with your Firebase project credentials from console.firebase.google.com
VITE_FIREBASE_API_KEY=YOUR_FIREBASE_API_KEY
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-app-id

# Google Analytics is intentionally not used (DPDP: no tracking before consent, none for children).

# Region the Cloud Functions are deployed to
VITE_FIREBASE_FUNCTIONS_REGION=us-central1

# App Check (NFR-3): reCAPTCHA v3 site key registered in Firebase Console → App Check
VITE_RECAPTCHA_V3_SITE_KEY=your-recaptcha-v3-site-key
# Local development only: debug token registered in App Check → Manage debug tokens
VITE_APPCHECK_DEBUG_TOKEN=

# DPDP grievance officer shown in the privacy notice
VITE_GRIEVANCE_OFFICER_NAME=Grievance Officer, NCERT Prep
VITE_GRIEVANCE_EMAIL=privacy@ncertprep.io

# Production builds fail without Firebase keys. Set to true only for a deliberate demo build (local data only).
VITE_ALLOW_DEMO_BUILD=
